kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins-devops-pvc
  namespace: devops
spec:
  storageClassName: nfs-share
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
